01_Control_LED_10
上传程序后，电路工作逻辑如下：LED只会在按键按下时点亮。这是一个最简单的模式和最简单的程序。
这个程序允许你将工作模式反转——仅当按键按下时，LED熄灭
下面是程序的文本片段，借助注释，你可以改变电路工作模式

反转
改变这个参数为 HIGH 可以使按键按下时，LED熄灭

01_Control_LED_11
上传程序后，电路工作逻辑如下：当按键按下时，LED点亮，当按键松开时，LED会在点亮3秒后自动熄灭。这是一个很棒的功能，如果你有时候忘记关灯的话。你可以自行设置一个时间后自动熄灭。
这个程序允许你设置松开按键后，LED继续点亮一个时间后自动熄灭。下面是程序的文本片段，借助注释，你可以改变电路工作模式

每次修改程序后，记得及时保存并上传到模块中